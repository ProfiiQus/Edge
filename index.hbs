{{!< default}}

<img src="{{asset "images/header.jpeg"}}" style="margin-bottom: 4vh"/>

<div class="content-area">
    <main class="site-main container">
        <div class="post-feed">
            <div class="grid-item grid-sizer"></div>
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </main>
</div>